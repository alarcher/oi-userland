--- libreoffice-6.4.3.2/configure.ac.orig	2020-05-19 17:58:06.273005591 +0000
+++ libreoffice-6.4.3.2/configure.ac	2020-05-19 17:58:11.469023552 +0000
@@ -605,7 +605,7 @@
 
     dnl Check whether we're using a SPARC or i386 processor
     AC_MSG_CHECKING([the processor type])
-    if test "$host_cpu" = "sparc" -o "$host_cpu" = "i386"; then
+    if test "$host_cpu" = "sparc" -o "$host_cpu" = "i386" -o "$host_cpu" = "x86_64"; then
         AC_MSG_RESULT([ok ($host_cpu)])
     else
         AC_MSG_ERROR([only SPARC and i386 processors are supported])
@@ -4577,7 +4577,7 @@
     P_SEP=:
 
     case "$host_cpu" in
-    i*86)
+    i*86|x86_64)
         CPUNAME=INTEL
         RTL_ARCH=x86
         PLATFORMID=solaris_x86
@@ -8191,7 +8191,8 @@
               solaris* | sunos* | sysv* | sco*)
                 case "$host_cpu" in
                   x86_64)
-                    objfmt='ELF64'
+                    # We want to build 32-bit for now
+                    objfmt='ELF'
                     ;;
                   *)
                     objfmt='ELF'
